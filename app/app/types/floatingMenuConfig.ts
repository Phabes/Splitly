import { MenuOption } from "./menuOption";

export type FloatingMenuConfig = {
  options: MenuOption[];
  trigger: { pageX: number; pageY: number; width: number; height: number };
};

export default function Index() {
  return null;
}
